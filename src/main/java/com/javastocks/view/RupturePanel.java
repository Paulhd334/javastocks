package com.javastocks.view;

import javax.swing.*;

public class RupturePanel extends JPanel {

    public RupturePanel(MainFrame frame) {
        // Exemple simple : juste un label pour identifier le panel
        add(new JLabel("Articles en Rupture Panel"));
    }
}